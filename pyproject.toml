[project]
name = "my-mini-agent"
version = "0.1.0"
description = "An AI agent running on your local machine"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "httpx>=0.28.1",
    "mcp>=1.27.1",
    "python-dotenv>=1.2.2",
    "rich>=15.0.0",
]
